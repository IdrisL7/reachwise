// ComparisonTable is a placeholder — the MDX markdown table handles the actual comparison display.
// This component exists to prevent MDX rendering errors when <ComparisonTable /> is used in posts.
export function ComparisonTable(_props: { competitor?: string }) {
  return null;
}
